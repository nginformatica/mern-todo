import { List, Map } from 'immutable';
import { expect } from 'chai';
import * as tasks from '../server/controller/task-controller';

describe('application logic', () => {
    describe('tasks', () => {
        it('just works :)', () => {
            expect(2 + 2).to.equal(4);
        });
    });
});
